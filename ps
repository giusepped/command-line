The ps command outputs a list of the process running at the moment when the command is called. Adding the options -x will
list all the processes currently running on the machine.

Example of usage. To list all the processes currently running on the machine:

ps -x
